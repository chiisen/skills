# Obsidian Bases Skill

此技能使支援 Skills 的 AI 助理能夠建立與編輯有效的 Obsidian Bases (`.base`) 檔案，包含視圖 (Views)、篩選器 (Filters)、公式 (Formulas) 及所有相關配置。

## 功能概述

**Obsidian Bases** 是基於 YAML 的檔案，用於定義 Obsidian 庫中筆記的動態視圖。一個 Base 檔案可以包含多個視圖、全域篩選器、公式、屬性配置以及自定義摘要。

### 核心能力

1.  **動態視圖**：支援表格 (Table)、卡片 (Cards)、列表 (List) 與地圖 (Map) 等多種視圖呈現方式。
2.  **強大的篩選系統**：支援巢狀的 `and`/`or`/`not` 邏輯，以及多樣化的運算子（如 `==`, `!=`, `>`, `file.hasTag()` 等）。
3.  **靈活的公式系統**：可以根據筆記內容、檔案元數據（如 `file.ctime`, `file.size`）或自定義邏輯計算新屬性。
4.  **數據摘要**：內建平均值 (Average)、總和 (Sum)、最大值 (Max)、最小值 (Min) 等統計功能，並支援自定義摘要公式。
5.  **視圖分組與排序**：可根據任一屬性對結果進行分組 (groupBy) 與排序 (order)。

## 使用說明

### 觸發場景
當您提及以下關鍵字或執行相關操作時，AI 將自動調用此技能：
- 建立或編輯 `.base` 檔案
- 使用「資料庫視圖」、「表格視圖」、「卡片視圖」
- 設定 Obsidian 中的篩選器或公式
- 提到「Obsidian Bases」

### 檔案格式
Bases 檔案使用 `.base` 副檔名，內部為標準的 YAML 格式。

### 嵌入方式
您可以在一般的 Markdown 檔案中嵌入 Base 視圖：
```markdown
![[MyBase.base]]

<!-- 嵌入特定視圖 -->
![[MyBase.base#View Name]]
```

## 範例配置：任務追蹤器 (Task Tracker)

以下是一個典型的 `.base` 檔案配置範例：

```yaml
filters:
  and:
    - file.hasTag("task")
    - 'file.ext == "md"'

formulas:
  days_until_due: 'if(due, ((date(due) - today()) / 86400000).round(0), "")'
  priority_label: 'if(priority == 1, "🔴 高", if(priority == 2, "🟡 中", "🟢 低"))'

properties:
  status:
    displayName: 狀態
  formula.priority_label:
    displayName: 優先級

views:
  - type: table
    name: "執行中任務"
    filters:
      and:
        - 'status != "done"'
    order:
      - file.name
      - status
      - formula.priority_label
      - due
    groupBy:
      property: status
      direction: ASC
```

## 相關參考
- [Bases Syntax](https://help.obsidian.md/bases/syntax)
- [Functions & Formulas](https://help.obsidian.md/formulas)
